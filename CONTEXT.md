# Domain Context

This is a TODO app. Users manage **tasks** — discrete units of work described by a **title**.
A task is either **pending** (not yet done) or **completed** (marked done by the user).
Tasks are never archived or permanently deleted from the UI — a delete action removes them immediately.
